// stub — full impl in Task 28
import React from 'react';
interface Props { onComplete: () => void; }
export function Onboarding(_props: Props) {
  return <div>Onboarding placeholder — Task 28</div>;
}
